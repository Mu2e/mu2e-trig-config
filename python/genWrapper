#! /usr/bin/env python
import sys
import os
import generateMenuFromJSON
menus      = ["physMenu","extrPosMenu", "valMenu"]
evtModes   = ["OnSpill","OffSpill","all"]
for m in menus:
    for evt in evtModes:
        menu_file  = '../data/{}.json'.format(m)
        out_dir    = sys.argv[1]
        os.system(f"mkdir -p {out_dir}")
        generateMenuFromJSON.generate(menu_file,
                                      evt,
                                      out_dir,
                                      False )
